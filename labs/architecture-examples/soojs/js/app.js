(function( window ) {
	'use strict';

	// SOOJS is a pattern so not a direct MVC framework
	//
	// for this example I will use JQuery for DOM access and updates
	// jQuery is not required in any way. It just happens to be the
	// simplest and most powerful path to dealing DOM damage
	var appViewController = new AppViewController(this, 'todoapp');


})( window );

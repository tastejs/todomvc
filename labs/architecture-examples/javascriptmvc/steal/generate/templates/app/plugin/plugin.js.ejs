steal('steal/less')
.then('./plugin.less', function(){
	var content = [];
	var timer;
	AddToContent = function(newContent){
		content.push(newContent);
		clearTimeout(timer);
		timer = setTimeout(function(){
			document.getElementById('content').innerHTML
				= content.join('');
		},30);
	};
});

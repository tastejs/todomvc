{
  "private": true,
  "devDependencies": {
    "polymer-cli": "^1.4.0"
  },
  "scripts": {
    "install": "bower install",
    "start": "python -m SimpleHTTPServer",
    "build": "polymer build && cp build/es5/bundle.src.html bundle.html"
  }
}

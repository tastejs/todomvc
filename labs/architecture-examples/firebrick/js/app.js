(function( window ) {
	'use strict';

	Firebrick.ready({
		app:{
			name:"Todo",
			path:"/js"
		},
		autoRender:false,
		require:["Todo.controller.TodoController"],
		go:function(){
			
		}
	});

})( window );

{
  "private": true,
  "scripts": {
    "compile": "duet --debug",
    "watch": "gazer -p 'src/**/*.js' npm run compile",
    "prepublish": "duet && rm public/index.html",
    "start": "npm install && npm run compile && npm run watch"
  },
  "dependencies": {
    "duet": "^1.6.1",
    "todomvc-app-css": "^2.0.1",
    "todomvc-common": "^1.0.2"
  },
  "devDependencies": {
    "gazer": "0.0.3"
  }
}
